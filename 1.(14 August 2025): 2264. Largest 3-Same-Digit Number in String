class Solution:
    def largestGoodInteger(self, num: str) -> str:
        largest = ""
        for i in range(len(num) - 2):
            triple = num[i:i+3]
            if triple[0] == triple[1] == triple[2]:
                if triple > largest:
                    largest = triple
        return largest
